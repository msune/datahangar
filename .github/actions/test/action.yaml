name: 'Hello World'
description: 'Greet someone'
inputs:
  ref:
    description: 'Github ref'
    required: true
runs:
  using: "composite"
  steps:
    - name: Checkout datahangar
      uses: actions/checkout@v4
      with:
        path: datahangar

    - name: Random Number Generator
      run: "echo Hello world ${{ inputs.ref }}"
      shell: bash
