name: 'Hello World'
description: 'Greet someone'
inputs:
  ref:
    description: 'Github ref'
    required: true
runs:
  using: "composite"
  steps:
    - name: Random Number Generator
      run: "Hello world ${{ inputs.ref }}"
      shell: bash
